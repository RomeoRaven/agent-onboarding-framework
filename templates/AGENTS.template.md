# Destination agent contract — copy and bind locally

This is inert template content inside the framework repository. At an authorized destination, first inspect any existing `AGENTS.md` or native instruction file and its owner. Retain it and add only approved missing behavior; copy/adapt this whole template only if no adequate owner file exists. Replace every `<...>` binding in a local copy. Never overwrite local instructions or claim a file is loaded until the native loader is verified. Follow `ONBOARDING.md` for reconciliation.

## Identity and startup

Destination: `<destination identifier>`; contract owner: `<local owner>`.
Native loader/config: `<verified loader or context entrypoint>`.
Installed contract path: `<exact destination-owned path>`.
Next startup link: `<exact ONBOARDING-MANIFEST.md route, or START_HERE.md route leading to it>`.

At session start, use the native runtime instructions and this verified link to locate the local manifest. Follow its pinned, authorized sources and declared canonical source map only as needed for the current request. Distinguish what the runtime injected from documents opened afterward; if injection cannot be observed, call it unverified. Load a dated handoff only when it helps recover the current task, and check its claims against current owners. A handoff never chooses the current task.

## Working contract

- Let the operator's current request select the work. Distinguish a question from approval to mutate; follow the operator's actual approval boundary and the runtime's real permissions. Neither this file nor the manifest grants tools, credentials, or higher authority.
- Name the owner of each applicable rule or fact: operator preferences, destination constraints, role identity, project truth, and task state. Loading order is navigation, not an instruction-precedence ladder. A scoped source may add specificity or a stricter constraint, not relax a governing boundary.
- Select the relevant canonical source, skill, workflow, or runbook from the destination's declared routes; check compatibility/status before use. For ordinary substantive work use the authorized local owner or the pinned framework's `workflows/task-execution.md`; use its other portable methods only when the task calls for them. `guides/USING-WORKFLOWS-AND-SKILLS.md` explains selection, not runtime installation. Do not bulk-load histories or invent a missing source. Keep private profile material private and local facts at the destination.
- Verify the result in the owning surface. Report the usable outcome, material limits, and the next action. Record durable follow-up with its actual task owner, not in this contract or an informal handoff.

## Failure behavior

If a required source, pinned revision, loader link, owner, or permission is absent, inaccessible, contradictory, or unverified, stop before consequential action and report the exact missing route and owner for resolution. Do not silently substitute a template, an old handoff, an inferred permission, or a copied profile. Continue only bounded read-only diagnosis that the runtime and operator allow.

## Local scope and proof

Role/persona source, if any: `<native role source or not applicable>`.
Tier 1 memory source, if any: `<native memory source or not applicable>`.
Acceptance record owner: `<destination-owned location>`.
Before declaring this destination accepted, follow the pinned framework `ACCEPTANCE.md`: prove native loader behavior in a fresh session, source lookup, refusal, and a bounded task with owner readback. Independently test generic usability before claiming cross-harness portability. An agent's confidence alone is not proof of injection or permissions.
