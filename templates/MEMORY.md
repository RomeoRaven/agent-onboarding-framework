# Optional Tier 1 memory pointer — destination-only example

Install only if this agent has a native, verified always-loaded memory store and needs a compact routing pointer. Otherwise omit this file. A template in the framework is not live memory.

Destination and memory owner: `<local owner>`.
Operator preference owner: `<authorized source route>`.
Local system and source map: `<destination-owned manifest/SYSTEM.md/SOURCES.md routes>`.
Selected method discovery: `<local workflow/skill index route or not applicable>`.
Open work owner: `<actual tracker route or not applicable>`.

Keep this store small and stable. Put reusable procedures in their workflow/skill owner and dated session continuity/evidence in selected Tier 3 records, not here. Verify current facts from their owners; a memory pointer grants no permission and cannot resolve a material authority conflict. Document whether this native runtime snapshots memory at session start or refreshes it live; do not assume either behavior across harnesses. Never install an empty memory file merely to mirror this example.
