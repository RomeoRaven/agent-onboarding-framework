# Destination startup pointer — install only when needed

This is a template, not an automatically loaded file. If the native agent contract can point directly to the manifest, omit this intermediate file. Otherwise place this short pointer where that contract explicitly links to it.

Destination: `<destination identifier>`.
Authoritative onboarding manifest: `<exact destination-owned path to ONBOARDING-MANIFEST.md>`.
Maintainer: `<local owner>`.

Open the manifest through this route, verify its required sources and pins, and use the current operator request to select work. The manifest owns the source map; do not restate its contents here. If this link is broken or the manifest cannot be verified, report the blocker and stop consequential work. This pointer grants no permissions and does not override the native runtime.
